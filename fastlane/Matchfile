git_url(ENV["MATCH_GIT_URL"])

storage_mode("git")

type("appstore")

app_identifier([
  "com.yeseullee.timerstamp",
  "com.yeseullee.timerstamp.TimerRemaining"
])

readonly(ENV["CI"] ? true : false)
